import 'package:flutter/material.dart';

class CreatePlayListAlert extends StatelessWidget {
  const CreatePlayListAlert({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}